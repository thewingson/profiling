spring.application.name=PROFILING-API
#spring.profiles.active=@activatedProperties@

#server configs
server.port=8082

# create and drop tables and sequences, loads import.sql
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false

# H2 settings
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost/pgdb_profiling}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:Kazakhstan1991}